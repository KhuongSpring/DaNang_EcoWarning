spring.config.import=optional:configserver:http://config-server:8888/

spring.datasource.url=jdbc:postgresql://postgresql:5432/auth_service
spring.datasource.username=${POSTGRES_USER:postgres}
spring.datasource.password=${POSTGRES_PASSWORD:Ntcuong@291}

spring.jpa.hibernate.ddl-auto=update

redis.host=redis
redis.port=6379
redis.password=root_admin

jwt.secret=${JWT_SECRET_KEY}
jwt.expiration.access=900000
jwt.expiration.refresh=604800000
jwt.expiration.otp=120000

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.from-name=DaNang EcoWarning System
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

eureka.client.service-url.defaultZone=http://discovery-server:8761/eureka/